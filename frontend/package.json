{
  "name": "ambient-panel-frontend",
  "version": "1.0.0",
  "description": "Ambient Panel Frontend - Svelte Application",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite dev --host 0.0.0.0",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.0.0",
    "svelte": "^4.2.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "date-fns": "^2.30.0"
  }
}
